import { FontcolorChange } from './fontcolor-change';

describe('FontcolorChange', () => {
  it('should create an instance', () => {
    const directive = new FontcolorChange();
    expect(directive).toBeTruthy();
  });
});
